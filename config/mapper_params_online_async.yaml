slam_toolbox:
  ros__parameters:

    # --- Core frames & topics ---
    odom_frame: odom          # From diff-drive plugin / odom topic
    map_frame: map
    base_frame: base_link     # Your robot's main body frame
    scan_topic: /scan         # From Gazebo lidar, bridged to ROS
    use_sim_time: true

    # --- Node behavior ---
    mode: mapping             # Use 'mapping' while building the map
    debug_logging: false
    throttle_scans: 1
    transform_publish_period: 0.02   # Publish map->odom at 50 Hz
    map_update_interval: 5.0         # Map publication period (s)

    # --- Laser range limits (tune to your world) ---
    min_laser_range: 0.12
    max_laser_range: 4.0

    # --- TF / cache timing ---
    transform_timeout: 0.3
    tf_buffer_duration: 30.0

    # --- General motion thresholds ---
    minimum_travel_distance: 0.2
    minimum_travel_heading: 0.2
    minimum_time_interval: 0.0

    # --- Scan buffer & distances ---
    scan_buffer_size: 10
    scan_buffer_maximum_scan_distance: 10.0
    link_match_minimum_response_fine: 0.1
    link_scan_maximum_distance: 1.5

    # --- Loop closure ---
    do_loop_closing: true
    loop_search_maximum_distance: 3.0
    loop_match_minimum_chain_size: 10
    loop_match_maximum_variance_coarse: 3.0
    loop_match_minimum_response_coarse: 0.35
    loop_match_minimum_response_fine: 0.45

    # --- Correlation search (local) ---
    correlation_search_space_dimension: 0.5
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.1

    # --- Correlation search (loop closure) ---
    loop_search_space_dimension: 8.0
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03

    # --- Scan matcher tuning ---
    distance_variance_penalty: 0.5
    angle_variance_penalty: 1.0
    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.349
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true

    # --- Map storage & interactive tools ---
    use_map_saver: true
    enable_interactive_mode: true
    stack_size_to_use: 40000000
    min_pass_through: 2
    occupancy_threshold: 0.1
